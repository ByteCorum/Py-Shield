name: MacOS tests

on:
  push:
    branches: [ stable, WIP ]
  pull_request:
    branches: [ stable, WIP ]

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Build Executable
      working-directory: scr
      run: |
        chmod +x build-mac.sh
        ./build-mac.sh
    - name: Archive Executable
      uses: actions/upload-artifact@v4
      with:
        name: py-shield-mac
        path: scr/py-shield

  obfuscation-multifile-macos:
    needs: ["build"]
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Executable
      uses: actions/download-artifact@v4
      with:
        name: py-shield-mac
        path: "examples"

    - name: Preparation For Tests
      working-directory: examples
      run: |
        mv py-shield multifile/
        chmod +x multifile/py-shield
        rm -rf multifile/obfuscated

    - name: Obfuscation
      working-directory: examples/multifile
      run: |
        ./py-shield obfuscate --aes --hashdata --fernet --chacha --salsa --base64 --recursive 4 --follow-imports --no-input --files lib.py main.py

    - name: Testing
      working-directory: examples/multifile/obfuscated
      run: |
        OUTPUT=$(python main.py)
        echo "Output: "
        echo "$OUTPUT"

        echo "Result: "
        if [ "$OUTPUT" = "hello world" ]; then
          echo "Success: main.py output contains 'hello world'"
        else
          echo "Failure: main.py output does not contain 'hello world'"
          exit 1
        fi

  obfuscation-onefile-macos:
    needs: ["build"]
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Executable
      uses: actions/download-artifact@v4
      with:
        name: py-shield-mac
        path: "examples"

    - name: Preparation For Tests
      working-directory: examples
      run: |
        mv py-shield onefile/
        chmod +x onefile/py-shield
        rm -rf onefile/obfuscated

    - name: Obfuscation
      working-directory: examples/onefile
      run: |
        ./py-shield obfuscate --aes --hashdata --fernet --chacha --salsa --base64 --recursive 4 --follow-imports --no-input main.py

    - name: Testing
      working-directory: examples/onefile/obfuscated
      run: |
        OUTPUT=$(python main.py)
        echo "Output: "
        echo "$OUTPUT"

        echo "Result: "
        if [ "$OUTPUT" = "hello world" ]; then
          echo "Success: main.py output contains 'hello world'"
        else
          echo "Failure: main.py output does not contain 'hello world'"
          exit 1
        fi

  obfuscationlegacy-multifile-macos:
    needs: ["build"]
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Executable
      uses: actions/download-artifact@v4
      with:
        name: py-shield-mac
        path: "examples"

    - name: Preparation For Tests
      working-directory: examples
      run: |
        mv py-shield multifile-legacy/
        chmod +x multifile-legacy/py-shield
        rm -rf multifile-legacy/obfuscated

    - name: Obfuscation
      working-directory: examples/multifile-legacy
      run: |
        ./py-shield obfuscatelegacy --no-input --mode 4 --loops 6 --files lib.py,main.py

    - name: Testing
      working-directory: examples/multifile-legacy/obfuscated
      run: |
        OUTPUT=$(python main.py)
        echo "Output: "
        echo "$OUTPUT"

        echo "Result: "
        if [ "$OUTPUT" = "hello world" ]; then
          echo "Success: main.py output contains 'hello world'"
        else
          echo "Failure: main.py output does not contain 'hello world'"
          exit 1
        fi

  obfuscationlegacy-onefile-macos:
    needs: ["build"]
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Download Executable
      uses: actions/download-artifact@v4
      with:
        name: py-shield-mac
        path: "examples"

    - name: Preparation For Tests
      working-directory: examples
      run: |
        mv py-shield onefile-legacy/
        chmod +x onefile-legacy/py-shield
        rm -rf onefile-legacy/obfuscated

    - name: Obfuscation
      working-directory: examples/onefile-legacy
      run: |
        ./py-shield obfuscatelegacy --no-input --mode 4 --loops 6 --files main.py

    - name: Testing
      working-directory: examples/onefile-legacy/obfuscated
      run: |
        OUTPUT=$(python main.py)
        echo "Output: "
        echo "$OUTPUT"

        echo "Result: "
        if [ "$OUTPUT" = "hello world" ]; then
          echo "Success: main.py output contains 'hello world'"
        else
          echo "Failure: main.py output does not contain 'hello world'"
          exit 1
        fi